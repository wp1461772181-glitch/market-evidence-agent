# 给 GPT-6 的开发启动提示词

先阅读同目录的 [V2 开发与验收方案](evidence-driven-forecast-v2-plan.md)。它是自包含的产品与执行规范；下面文本可以直接复制到有本项目文件访问能力的 GPT-6/Codex 任务。

```text
请在 /Users/wupeng/monash-ai/projects/market-evidence-agent 开发 Market Evidence Agent V2。

完整执行规范是 docs/evidence-driven-forecast-v2-plan.md。请先完整阅读，再核对实际 HEAD、工作树、适用 AGENTS.md 和已存在的 docs/v2-progress.md。不要依赖旧聊天或把方案里的待新增接口当成已存在。

目标：把行情、官方财报/手动媒体、正反证据研究、联合数值预测、固定目标修订、每小时官方监控和到期评估连成可验证系统。证据必须真实进入联合模型；不能再复制旧概率后附上文字，就宣称修订完成。

从第一个未验收阶段开始，按 P0–P8 实施。本轮先完成 P0，再推进没有阻塞的下一阶段；如果单轮上下文不足，把真实进度与下一步写入 docs/v2-progress.md，后续从那里继续。不要把整份系统宣称成单轮必定完成，也不要只给新计划而不执行。

保留现有数据和旧档案，使用增量迁移。默认20个交易日、绝对收盘价收益、横盘±2%；旧模型预测SPY相对收益，必须重建相应数据与模型，不能只改标签文字。修订保持原基准价和目标日，自动窗口以原根决策时间算72小时，子版不重置窗口。

优先复用现有 Python/React/PostgreSQL/逻辑回归/DeepSeek。不要引入微服务或复杂Agent框架。主代理负责规划、集成与独立验收；有独立文件边界的实现任务交给子代理，避免同时编辑同一文件。

不读取或输出个人密码文件与密钥；已有提供方配置通过现有加载器使用。真实文档/API调用遵守方案小批上限，超预算不要自行扩大。除破坏性操作或真实信息/预算阻塞外，不重复询问已确定事项。

每阶段完成必要测试、浏览器/真实流程验收和 Git 提交，更新 docs/v2-progress.md；按本任务授权把通过验收的提交推送到既有GitHub仓库并核验远端SHA。不得覆盖用户并行修改。

模型不达门槛时明确标记 research_only 或 blocked，不能手工凑出“证据概率”。继续推进不依赖该门槛的部分。官方融合和媒体融合分别验收；不能把只有官方支持的版本说成全部完成。

每次报告写清：已验证、部分完成、未验证、阻塞；给出文件、测试和真实结果证据。最后用A01–A20矩阵逐项验收，并分别报告工程闭环、模型离线门槛和真实前向效果。
```

若 GPT-6 无法访问本地项目，把本文件和 `evidence-driven-forecast-v2-plan.md` 一起提供，并连接现有 GitHub 仓库。只提供这段提示词而不提供主文档不足以独立开发。

## 当前交接状态

- 代码基线：`a135bf9ca983008b5690dd254358e3aba06e12ae`。
- 本次只编写方案与交接说明，没有实施任何 P0–P8 功能。
- 本方案引入的预测口径、数据模型、接口与发布门槛均属于后续开发目标。
- 已有每小时监控是本机调度，现有自动证据修订保持原概率。必须按主方案改造，不能将已有任务当作V2已完成。
